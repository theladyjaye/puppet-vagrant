<%= ERB.new(File.read('/home/vagrant/.profile')).result() %>

# profile additions
if [ -d "$HOME/profile.d" ] ; then
    for conf in $(ls -1 $HOME/profile.d)
    do
        . "$HOME/profile.d/$conf"
    done
fi
